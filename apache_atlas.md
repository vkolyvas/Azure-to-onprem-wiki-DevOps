# Apache Atlas – Installation & Configuration
**Installation**

1. Install dependencies (Java, Kafka, HBase).

2. Download Atlas binary.

3. Start services:

```./bin/atlas_start.py```

**Configuration**

- Configure metadata types

- Configure authentication (LDAP/Kerberos)
